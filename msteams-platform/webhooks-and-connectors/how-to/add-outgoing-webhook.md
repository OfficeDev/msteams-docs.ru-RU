---
title: Добавление пользовательских ботов в Microsoft Teams с помощью исходяющих веб-hooks
description: описывает, как добавить исходяющий веб-hook
ms.topic: conceptual
ms.author: lajanuar
keywords: Teams вкладок исходя сообщений с действиями веб-хостинга для проверки веб-hook
ms.openlocfilehash: 20d30be7a35b33b94dc4a856439c51d5d0cc441c
ms.sourcegitcommit: f74b74d5bed1df193e59f46121ada443fb57277b
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 02/03/2021
ms.locfileid: "50093255"
---
# <a name="add-custom-bots-to-teams-with-outgoing-webhooks"></a><span data-ttu-id="cd559-104">Добавление пользовательских ботов в Teams с помощью исходяющих веб-hooks</span><span class="sxs-lookup"><span data-stu-id="cd559-104">Add custom bots to Teams with outgoing webhooks</span></span>

## <a name="what-are-outgoing-webhooks-in-teams"></a><span data-ttu-id="cd559-105">Что такое исходяющие веб-hooks в Teams?</span><span class="sxs-lookup"><span data-stu-id="cd559-105">What are outgoing webhooks in Teams?</span></span>

<span data-ttu-id="cd559-106">Веб-hooks — это объединяющий способ интеграции Teams с внешними приложениями.</span><span class="sxs-lookup"><span data-stu-id="cd559-106">Webhooks are an eminent way for Teams to integrate with external apps.</span></span> <span data-ttu-id="cd559-107">Веб-hook — это, по сути, запрос POST, отправленный на URL-адрес для вызова.</span><span class="sxs-lookup"><span data-stu-id="cd559-107">A webhook is essentially a POST request sent to a callback URL.</span></span> <span data-ttu-id="cd559-108">Исходяющие веб-hooks позволяют пользователям отправлять сообщения в веб-службу без полного процесса создания ботов с помощью [Microsoft Bot Framework.](https://dev.botframework.com/)</span><span class="sxs-lookup"><span data-stu-id="cd559-108">Outgoing webhooks allow users to send messages to your web service without going through the full process of creating bots via the [Microsoft Bot Framework](https://dev.botframework.com/).</span></span>

<span data-ttu-id="cd559-109">Исходяющий веб-браузер отправляет данные из Teams любой выбранной службе, способной принимать полезное количество данных JSON.</span><span class="sxs-lookup"><span data-stu-id="cd559-109">Outgoing webhook sends data from Teams to any chosen service capable of accepting a JSON payload.</span></span> <span data-ttu-id="cd559-110">После добавления исходяющих веб-hooks в команду он действует как бот и ищет сообщения в каналах, используя **\@ упоминание.**</span><span class="sxs-lookup"><span data-stu-id="cd559-110">After adding the outgoing webhooks to a team, it acts as a bot and looks for messages in channels using **\@mention**.</span></span> <span data-ttu-id="cd559-111">Она отправляет уведомления внешним веб-службам и отвечает на них с помощью сообщений, в том числе карточек и изображений.</span><span class="sxs-lookup"><span data-stu-id="cd559-111">It sends notifications to external web services and responds with rich messages, which include cards and images.</span></span>

## <a name="outgoing-webhook-key-features"></a><span data-ttu-id="cd559-112">Ключевые функции исходяющих веб-функций веб-хостинга</span><span class="sxs-lookup"><span data-stu-id="cd559-112">Outgoing webhook key features</span></span>

| <span data-ttu-id="cd559-113">Возможность</span><span class="sxs-lookup"><span data-stu-id="cd559-113">Feature</span></span> | <span data-ttu-id="cd559-114">Описание</span><span class="sxs-lookup"><span data-stu-id="cd559-114">Description</span></span> |
| ------- | ----------- |
| <span data-ttu-id="cd559-115">Конфигурация с областью действия</span><span class="sxs-lookup"><span data-stu-id="cd559-115">Scoped configuration</span></span>| <span data-ttu-id="cd559-116">Область действия веб-hooks на уровне группы.</span><span class="sxs-lookup"><span data-stu-id="cd559-116">Webhooks are scoped at the team level.</span></span> <span data-ttu-id="cd559-117">Необходимо пройти процедуру настройки для каждой команды, в которую нужно добавить исходяющий веб-проект.</span><span class="sxs-lookup"><span data-stu-id="cd559-117">You must go through the setup process for each team where you want to add your outgoing webhook.</span></span> |
| <span data-ttu-id="cd559-118">Реактивное обмен сообщениями</span><span class="sxs-lookup"><span data-stu-id="cd559-118">Reactive messaging</span></span>| <span data-ttu-id="cd559-119">Пользователи должны использовать @mention для получения сообщений веб-пользователем.</span><span class="sxs-lookup"><span data-stu-id="cd559-119">Users must use @mention for the webhook to receive messages.</span></span> <span data-ttu-id="cd559-120">В настоящее время пользователи могут отправлять сообщения только исходяшему веб-hook в общедоступных каналах, но не в пределах личных или частных областей.</span><span class="sxs-lookup"><span data-stu-id="cd559-120">Currently, users can only message an outgoing webhook in public channels and not within the personal or private scope.</span></span> |
|<span data-ttu-id="cd559-121">Стандартный обмен сообщениями HTTP</span><span class="sxs-lookup"><span data-stu-id="cd559-121">Standard HTTP message exchange</span></span>|<span data-ttu-id="cd559-122">Ответы отображаются в той же цепочке, что и исходное сообщение запроса, и могут включать любое содержимое сообщения на базе бота, например, текст, изображения, карточки и эмодзи.</span><span class="sxs-lookup"><span data-stu-id="cd559-122">Responses appear in the same chain as the original request message and can include any bot framework message content, for example, rich text, images, cards, and emojis.</span></span> <span data-ttu-id="cd559-123">Несмотря на то что исходяющие веб-hooks могут использовать карточки, они не могут использовать какие-либо действия с карточками, кроме `openURL` .</span><span class="sxs-lookup"><span data-stu-id="cd559-123">Although outgoing webhooks can use cards, they cannot use any card actions except for `openURL`.</span></span>|
| <span data-ttu-id="cd559-124">Поддержка методов API Teams</span><span class="sxs-lookup"><span data-stu-id="cd559-124">Teams API method support</span></span>|<span data-ttu-id="cd559-125">Исходяющий веб-браузер отправляет HTTP POST веб-службе и обрабатывает ответ обратно.</span><span class="sxs-lookup"><span data-stu-id="cd559-125">Outgoing webhook sends an HTTP POST to a web service and process a response back.</span></span> <span data-ttu-id="cd559-126">Они не могут получить доступ к другим API, таким как извлечение списка или списка каналов в команде.</span><span class="sxs-lookup"><span data-stu-id="cd559-126">They cannot access any other APIs like retrieve the roster or list of channels in a team.</span></span>|

## <a name="creating-actionable-messages"></a><span data-ttu-id="cd559-127">Создание сообщений с действиями</span><span class="sxs-lookup"><span data-stu-id="cd559-127">Creating actionable messages</span></span>

<span data-ttu-id="cd559-128">Карточки соединители включают три видимых кнопки на карточке.</span><span class="sxs-lookup"><span data-stu-id="cd559-128">The connector cards include three visible buttons on the card.</span></span> <span data-ttu-id="cd559-129">Каждая кнопка определяется в `potentialAction` свойстве сообщения с помощью `ActionCard` действий.</span><span class="sxs-lookup"><span data-stu-id="cd559-129">Each button is defined in the `potentialAction` property of the message by using `ActionCard` actions.</span></span> <span data-ttu-id="cd559-130">Каждый из них содержит тип ввода, текстовое поле, выбор даты или список с `ActionCard` несколькими вариантами выбора.</span><span class="sxs-lookup"><span data-stu-id="cd559-130">Each `ActionCard` contains an input type; a text field, a date picker, or a multi-choice list.</span></span> <span data-ttu-id="cd559-131">Каждое `ActionCard` действие имеет связанное действие, `HttpPOST` например.</span><span class="sxs-lookup"><span data-stu-id="cd559-131">Each `ActionCard` action has an associated action, for example, `HttpPOST`.</span></span>

<span data-ttu-id="cd559-132">Карточки соединителей поддерживают действия трех типов:</span><span class="sxs-lookup"><span data-stu-id="cd559-132">Connector cards support three types of actions:</span></span>

| <span data-ttu-id="cd559-133">Действие</span><span class="sxs-lookup"><span data-stu-id="cd559-133">Action</span></span> | <span data-ttu-id="cd559-134">Описание</span><span class="sxs-lookup"><span data-stu-id="cd559-134">Description</span></span> |
| ------- | ----------- |
| `ActionCard` |<span data-ttu-id="cd559-135">Представляет один или несколько типов ввода и связанных действий.</span><span class="sxs-lookup"><span data-stu-id="cd559-135">Presents one or more input types and associated actions.</span></span>|
| `HttpPOST` | <span data-ttu-id="cd559-136">Отправляет запрос POST на URL-адрес.</span><span class="sxs-lookup"><span data-stu-id="cd559-136">Sends a POST request to a URL.</span></span> |
| `OpenUri` |  <span data-ttu-id="cd559-137">Открывает URI в отдельном браузере или приложении, при желании он ориентирован на различные URI на основе операционных систем.</span><span class="sxs-lookup"><span data-stu-id="cd559-137">Opens a URI in a separate browser or app, optionally targets different URIs based on operating systems.</span></span>|

<span data-ttu-id="cd559-138">Действие `ActionCard` поддерживает три типа входных данных:</span><span class="sxs-lookup"><span data-stu-id="cd559-138">The `ActionCard` action supports three input types:</span></span>

| <span data-ttu-id="cd559-139">Тип ввода</span><span class="sxs-lookup"><span data-stu-id="cd559-139">Input type</span></span> | <span data-ttu-id="cd559-140">Описание</span><span class="sxs-lookup"><span data-stu-id="cd559-140">Description</span></span> |
| ------- | ----------- |
| `TextInput` | <span data-ttu-id="cd559-141">Однострочная или многострочная текстовая поля с необязательным ограничением длины.</span><span class="sxs-lookup"><span data-stu-id="cd559-141">A single-line or multiline text field with an optional length limit.</span></span> |
| `DateInput` | <span data-ttu-id="cd559-142">Выбор даты с необязательным селектором времени.</span><span class="sxs-lookup"><span data-stu-id="cd559-142">A date selector with an optional time selector.</span></span> |
| `MultichoiceInput` | <span data-ttu-id="cd559-143">Указанный список вариантов, предлагающий один или несколько вариантов выбора.</span><span class="sxs-lookup"><span data-stu-id="cd559-143">A specified list of choices, offering either a single selection or multiple selections.</span></span>|

<span data-ttu-id="cd559-144">`MultichoiceInput` поддерживает `style` свойство, которое управляет отображением полного списка.</span><span class="sxs-lookup"><span data-stu-id="cd559-144">`MultichoiceInput` supports a `style` property that controls the display of a fully expanded list.</span></span> <span data-ttu-id="cd559-145">Значение по умолчанию `style` зависит от `isMultiSelect` значения.</span><span class="sxs-lookup"><span data-stu-id="cd559-145">The default value of `style` depends on the `isMultiSelect` value.</span></span>

| <span data-ttu-id="cd559-146">`isMultiSelect` value</span><span class="sxs-lookup"><span data-stu-id="cd559-146">`isMultiSelect` value</span></span>  | <span data-ttu-id="cd559-147">`style` значение по умолчанию</span><span class="sxs-lookup"><span data-stu-id="cd559-147">`style` default value</span></span>  |
| --- | --- |
| <span data-ttu-id="cd559-148">`false` или не задано</span><span class="sxs-lookup"><span data-stu-id="cd559-148">`false` or not specified</span></span> | <span data-ttu-id="cd559-149">Стиль по умолчанию: `compact`</span><span class="sxs-lookup"><span data-stu-id="cd559-149">The default style is `compact`</span></span>|
| `true` | <span data-ttu-id="cd559-150">Стиль по умолчанию: `expanded`</span><span class="sxs-lookup"><span data-stu-id="cd559-150">The default style is `expanded`</span></span> |

> [!NOTE]
> * <span data-ttu-id="cd559-151">Введите и то, и другое, если вы хотите, чтобы список с несколькими выборами отображался `"isMultiSelect": true` `"style": true` в компактном стиле.</span><span class="sxs-lookup"><span data-stu-id="cd559-151">Enter both `"isMultiSelect": true` and `"style": true`, if you want the multi-select list to be displayed in a compact style.</span></span>
> * <span data-ttu-id="cd559-152">Выбор свойства в Teams такой же, как и выбор свойства `compact` `style` в Microsoft `normal` `style` Outlook.</span><span class="sxs-lookup"><span data-stu-id="cd559-152">Selecting `compact` for the `style` property in Teams is the same as selecting `normal` for the `style` property in Microsoft Outlook.</span></span>
> * <span data-ttu-id="cd559-153">Веб-пользователи поддерживают только карточки "Назад" и адаптивные карточки сообщений Office 365.</span><span class="sxs-lookup"><span data-stu-id="cd559-153">Webhooks support only Office 365 message back cards and adaptive cards.</span></span>

<span data-ttu-id="cd559-154">Все остальные сведения о действиях **[](/outlook/actionable-messages/card-reference#actions)** с карточками соединители см. в справке по карточкам сообщений с действиями.</span><span class="sxs-lookup"><span data-stu-id="cd559-154">For all other details about connector card actions, see **[Actions](/outlook/actionable-messages/card-reference#actions)** in the actionable message card reference.</span></span>

## <a name="adding-outgoing-webhooks-to-your-app"></a><span data-ttu-id="cd559-155">Добавление исходяющих веб-функций в приложение</span><span class="sxs-lookup"><span data-stu-id="cd559-155">Adding outgoing webhooks to your app</span></span>

<span data-ttu-id="cd559-156">**Сценарий:** push-уведомления об изменениях на сервере базы данных каналов Teams в ваше приложение.</span><span class="sxs-lookup"><span data-stu-id="cd559-156">**Scenario**: Push change status notifications on a Teams channel database server to your app.</span></span>  
<span data-ttu-id="cd559-157">**Пример:** у вас есть бизнес-приложение, которое отслеживает все операции CRUD, сделанные с записями сотрудников пользователями отдела кадров канала Teams в области аренды Office 365.</span><span class="sxs-lookup"><span data-stu-id="cd559-157">**Example**: You have a line-of-business app that tracks all CRUD operations made to employee records by Teams channel HR users across an Office 365 tenancy.</span></span>

### <a name="1-create-a-url-on-your-apps-server-to-accept-and-process-a-post-request-with-a-json-payload"></a><span data-ttu-id="cd559-158">1. Создайте URL-адрес на сервере приложения, чтобы принять и обработать запрос POST с помощью полезной нагрузки JSON</span><span class="sxs-lookup"><span data-stu-id="cd559-158">1. Create a URL on your app's server to accept and process a POST request with a JSON payload</span></span>

<span data-ttu-id="cd559-159">Ваша служба получает сообщения в стандартной схеме сообщений службы ботов Azure.</span><span class="sxs-lookup"><span data-stu-id="cd559-159">Your service receives messages in a standard Azure bot service messaging schema.</span></span> <span data-ttu-id="cd559-160">Соединитель платформы ботов — это служба RESTful, которая позволяет службе обрабатывать обмен сообщениями в формате JSON по протоколам HTTPS, как описано в API службы ботов [Azure.](/bot-framework/rest-api/bot-framework-rest-connector-api-reference)</span><span class="sxs-lookup"><span data-stu-id="cd559-160">The bot framework connector is a RESTful service that empowers your service to process the interchange of JSON formatted messages via HTTPS protocols as documented in the [Azure Bot Service API](/bot-framework/rest-api/bot-framework-rest-connector-api-reference).</span></span> <span data-ttu-id="cd559-161">Кроме того, вы можете использовать [Microsoft Bot Framework SDK] для обработки и обработки сообщений.</span><span class="sxs-lookup"><span data-stu-id="cd559-161">Alternatively, you can follow the [Microsoft Bot Framework SDK] to process and parse messages.</span></span> <span data-ttu-id="cd559-162">См. также [о службе ботов Azure.](/azure/bot-service/bot-service-overview-introduction)</span><span class="sxs-lookup"><span data-stu-id="cd559-162">See also [About Azure Bot Service](/azure/bot-service/bot-service-overview-introduction).</span></span>


<span data-ttu-id="cd559-163">Исходяющие веб-hooks имеют область действия на уровне и видны `team` всем участникам команды.</span><span class="sxs-lookup"><span data-stu-id="cd559-163">Outgoing webhooks are scoped to the `team` level and are visible to all the team members.</span></span> <span data-ttu-id="cd559-164">Как и бот, пользователям **\@** необходимо упомянуть имя исходя из веб-пользователя, чтобы вызвать его в канале.</span><span class="sxs-lookup"><span data-stu-id="cd559-164">Just like a bot, users need to **\@mention** the name of the outgoing webhook to invoke it in the channel.</span></span>

### <a name="2-create-a-method-to-verify-the-outgoing-webhook-hmac-token"></a><span data-ttu-id="cd559-165">2. Создайте метод для проверки маркера HMAC исходярия веб-ook</span><span class="sxs-lookup"><span data-stu-id="cd559-165">2. Create a method to verify the outgoing webhook HMAC token</span></span>

#### <a name="hmac-signature-for-testing-with-code-example"></a><span data-ttu-id="cd559-166">Подпись HMAC для тестирования с помощью примера кода</span><span class="sxs-lookup"><span data-stu-id="cd559-166">HMAC signature for testing with code example</span></span>

<span data-ttu-id="cd559-167">Пример входящие сообщения и ид: "contoso" из SigningKeyDictionary {"contoso", "vqF0En+Z0ucuRTM/01o2GuhMH3hKKk/N2bOmlM31zaA=" }.</span><span class="sxs-lookup"><span data-stu-id="cd559-167">Using example of inbound message and id: "contoso" of SigningKeyDictionary of {"contoso", "vqF0En+Z0ucuRTM/01o2GuhMH3hKKk/N2bOmlM31zaA=" }.</span></span>

<span data-ttu-id="cd559-168">Используйте значение HMAC 03TCao0i55H1eVKUusZOTZRjtvYTs+mO41mPL+R1e1U=" в авторизации загона запроса.</span><span class="sxs-lookup"><span data-stu-id="cd559-168">Use the value "HMAC 03TCao0i55H1eVKUusZOTZRjtvYTs+mO41mPL+R1e1U=" in the authorization of request header.</span></span>

<span data-ttu-id="cd559-169">Чтобы убедиться, что служба получает звонки только от реальных клиентов Teams, Teams предоставляет код HMAC в `hmac` http-заговорке.</span><span class="sxs-lookup"><span data-stu-id="cd559-169">To ensure that your service is receiving calls only from actual Teams clients, Teams provides an HMAC code in the HTTP `hmac` header.</span></span> <span data-ttu-id="cd559-170">Код всегда включается в протокол проверки подлинности.</span><span class="sxs-lookup"><span data-stu-id="cd559-170">Always included the code in your authentication protocol.</span></span>

<span data-ttu-id="cd559-171">Код всегда должен проверять подпись HMAC, включенную в запрос:</span><span class="sxs-lookup"><span data-stu-id="cd559-171">Your code must always validate the HMAC signature included in the request:</span></span>

* <span data-ttu-id="cd559-172">Создание маркера HMAC из тела запроса сообщения.</span><span class="sxs-lookup"><span data-stu-id="cd559-172">Generate the HMAC token from the request body of the message.</span></span> <span data-ttu-id="cd559-173">На большинстве платформ это можно сделать в стандартных библиотеках (см. "Crypto [for](https://nodejs.org/api/crypto.html#crypto_crypto) Node.js or see [Teams Webhook Sample](https://github.com/OfficeDev/microsoft-teams-sample-outgoing-webhook/blob/23eb61da5a18634d51c5247944843da9abed01b6/WebhookSampleBot/Models/AuthProvider.cs) for C"). \#</span><span class="sxs-lookup"><span data-stu-id="cd559-173">There are standard libraries to do this on most platforms (see [Crypto](https://nodejs.org/api/crypto.html#crypto_crypto) for Node.js or see [Teams Webhook Sample](https://github.com/OfficeDev/microsoft-teams-sample-outgoing-webhook/blob/23eb61da5a18634d51c5247944843da9abed01b6/WebhookSampleBot/Models/AuthProvider.cs) for C\#).</span></span> <span data-ttu-id="cd559-174">Microsoft Teams использует стандартное шифрование HMAC SHA256.</span><span class="sxs-lookup"><span data-stu-id="cd559-174">Microsoft Teams uses standard SHA256 HMAC cryptography.</span></span> <span data-ttu-id="cd559-175">В UTF8 необходимо преобразовать тело в массив byte.</span><span class="sxs-lookup"><span data-stu-id="cd559-175">You need to convert the body to a byte array in UTF8.</span></span>
* <span data-ttu-id="cd559-176">Вычислите hash из массива byte маркера безопасности, предоставленного **Teams** при регистрации исходяшего веб-hook в клиенте Teams].</span><span class="sxs-lookup"><span data-stu-id="cd559-176">Compute the hash from the byte array of the security token **provided by Teams** when you registered the outgoing webhook in the Teams client].</span></span> <span data-ttu-id="cd559-177">См. [статью "Создание исходяного веб-хостинга".](#create-an-outgoing-webhook)</span><span class="sxs-lookup"><span data-stu-id="cd559-177">See [Create an outgoing webhook](#create-an-outgoing-webhook).</span></span>
* <span data-ttu-id="cd559-178">Преобразуем hash в строку с помощью кодификации UTF-8.</span><span class="sxs-lookup"><span data-stu-id="cd559-178">Convert the hash to a string using UTF-8 encoding.</span></span>
* <span data-ttu-id="cd559-179">Сравните строку значения сгенерированного hash со значением, предоставленным в HTTP-запросе.</span><span class="sxs-lookup"><span data-stu-id="cd559-179">Compare the string value of the generated hash with the value provided in the HTTP request.</span></span>

### <a name="3-create-a-method-to-send-a-success-or-failure-response"></a><span data-ttu-id="cd559-180">3. Создайте метод для отправки ответа об успешном или неудаче</span><span class="sxs-lookup"><span data-stu-id="cd559-180">3. Create a method to send a success or failure response</span></span>

<span data-ttu-id="cd559-181">Ответы от исходяющих веб-пользователей отображаются в той же цепочке ответов, что и исходное сообщение.</span><span class="sxs-lookup"><span data-stu-id="cd559-181">Responses from your outgoing webhooks appear in the same reply chain as the original message.</span></span> <span data-ttu-id="cd559-182">Когда пользователь выполняет запрос, Microsoft Teams выполняет синхронный HTTP-запрос для вашей службы, и ваш код получает пять секунд на ответ на сообщение до того, как соединение будет прервано.</span><span class="sxs-lookup"><span data-stu-id="cd559-182">When the user performs a query, Microsoft Teams issues a synchronous HTTP request to your service and your code gets five seconds to respond to the message before the connection times out and terminates.</span></span>

### <a name="example-response"></a><span data-ttu-id="cd559-183">Пример отклика</span><span class="sxs-lookup"><span data-stu-id="cd559-183">Example response</span></span>

```json
{
    "type": "message",
    "text": "This is a reply!"
}
```

## <a name="create-an-outgoing-webhook"></a><span data-ttu-id="cd559-184">Создание исходящего веб-перехватчика</span><span class="sxs-lookup"><span data-stu-id="cd559-184">Create an outgoing webhook</span></span>

1. <span data-ttu-id="cd559-185">Выберите соответствующую команду и выберите команду **"Управление"** в &#8226;&#8226;&#8226; меню (&#8226;&#8226;&#8226;).</span><span class="sxs-lookup"><span data-stu-id="cd559-185">Select the appropriate team and choose **Manage team** from the (&#8226;&#8226;&#8226;) drop-down menu.</span></span>
1. <span data-ttu-id="cd559-186">Выберите **вкладку "Приложения"** на панели навигации.</span><span class="sxs-lookup"><span data-stu-id="cd559-186">Choose the **Apps** tab from the navigation bar.</span></span>
1. <span data-ttu-id="cd559-187">В правом нижнем углу окна выберите **"Создать исходяющий веб-hook".**</span><span class="sxs-lookup"><span data-stu-id="cd559-187">From the window's lower right corner select **Create an outgoing webhook**.</span></span>
1. <span data-ttu-id="cd559-188">В итоговом всплывающее окно заполняют необходимые поля:</span><span class="sxs-lookup"><span data-stu-id="cd559-188">In the resulting popup window complete the required fields:</span></span>

>* <span data-ttu-id="cd559-189">**Имя** — заголовок веб-@mention коснитесь.</span><span class="sxs-lookup"><span data-stu-id="cd559-189">**Name** - The webhook title and @mention tap.</span></span>
>* <span data-ttu-id="cd559-190">**URL-адрес для повторного** вызова — конечная точка HTTPS, которая принимает полезное сообщение JSON и получает запросы POST от Teams.</span><span class="sxs-lookup"><span data-stu-id="cd559-190">**Callback URL** - The HTTPS endpoint that accepts JSON payloads and receives POST requests from Teams.</span></span>
>* <span data-ttu-id="cd559-191">**Описание** — подробная строка, которая появляется в карточке профиля и панели мониторинга приложения на уровне группы.</span><span class="sxs-lookup"><span data-stu-id="cd559-191">**Description** - A detailed string that appear in the profile card and the team-level App dashboard.</span></span>
>* <span data-ttu-id="cd559-192">**Изображение профиля —** необязательный значок приложения для веб-пользователя.</span><span class="sxs-lookup"><span data-stu-id="cd559-192">**Profile Picture** an optional app icon for your webhook.</span></span>
>* <span data-ttu-id="cd559-193">Выберите **кнопку "Создать"** в правом нижнем углу всплывающее окно, и исходяющий веб-hook будет добавлен в каналы текущей команды.</span><span class="sxs-lookup"><span data-stu-id="cd559-193">Select the **Create** button from the lower right corner of the pop-up window and the outgoing webhook are added to the current team's channels.</span></span>
>* <span data-ttu-id="cd559-194">В следующем диалоговом окне отображается маркер безопасности на основе хэш-кода проверки подлинности [сообщений (HMAC),](https://security.stackexchange.com/questions/20129/how-and-when-do-i-use-hmac/20301) который используется для проверки подлинности вызовов между Teams и назначенной внешней службой.</span><span class="sxs-lookup"><span data-stu-id="cd559-194">The next dialog window displays an [Hash-based Message Authentication Code (HMAC)](https://security.stackexchange.com/questions/20129/how-and-when-do-i-use-hmac/20301) security token that is used to authenticate calls between Teams and the designated outside service.</span></span>
>* <span data-ttu-id="cd559-195">Исходяющий веб-пользователь доступен пользователям команды, только если URL-адрес действителен, а маркеры проверки подлинности сервера и клиента равны, например, для HMAC.</span><span class="sxs-lookup"><span data-stu-id="cd559-195">The outgoing webhook is available to the team's users, only if the URL is valid and the server and client authentication tokens are equal for example, an HMAC handshake.</span></span>

## <a name="code-samples"></a><span data-ttu-id="cd559-196">Примеры кода</span><span class="sxs-lookup"><span data-stu-id="cd559-196">Code samples</span></span>

<span data-ttu-id="cd559-197">На GitHub можно просмотреть примеры кода исходяющих веб-пользователей:</span><span class="sxs-lookup"><span data-stu-id="cd559-197">You can view outgoing webhook code samples on GitHub:</span></span>

### <a name="nodejs"></a><span data-ttu-id="cd559-198">Node.js</span><span class="sxs-lookup"><span data-stu-id="cd559-198">Node.js</span></span>

[<span data-ttu-id="cd559-199">OfficeDev/msteams-samples-outgoing-webhook-nodejs</span><span class="sxs-lookup"><span data-stu-id="cd559-199">OfficeDev/msteams-samples-outgoing-webhook-nodejs</span></span>](https://github.com/OfficeDev/msteams-samples-outgoing-webhook-nodejs)

### <a name="c"></a><span data-ttu-id="cd559-200">C\#</span><span class="sxs-lookup"><span data-stu-id="cd559-200">C\#</span></span>

[<span data-ttu-id="cd559-201">OfficeDev/microsoft-teams-sample-outgoing-webhook</span><span class="sxs-lookup"><span data-stu-id="cd559-201">OfficeDev/microsoft-teams-sample-outgoing-webhook</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-outgoing-webhook)
